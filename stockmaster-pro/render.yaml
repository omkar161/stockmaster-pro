services:
  - type: web
    name: stockmaster-pro-backend
    env: node
    region: oregon
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      NODE_VERSION: ~18